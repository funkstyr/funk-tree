{
  "name": "@funk-tree/crawler",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "crawl": "bun run src/index.ts crawl",
    "migrate": "bun run src/migrate.ts",
    "status": "bun run src/index.ts status",
    "geocode": "bun run src/index.ts geocode",
    "export": "bun run src/index.ts export",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@funk-tree/db": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@funk-tree/config": "workspace:*",
    "@types/bun": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
